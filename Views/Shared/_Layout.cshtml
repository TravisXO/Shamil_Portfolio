<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="google-site-verification" content="8JdmuGQJ3LPSbCMq6HHCpx-Jf5SZaJlP89KDWoGgfbc" />
    <title>@ViewData["Title"] - Shamil_Portfolio</title>

    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/hero.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/Shamil_Portfolio.styles.css" asp-append-version="true" />

    <link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@300;400;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style type="text/css">
        * {
            cursor: none !important;
        }
    </style>

</head>
<body class="portfolio-body">

    <div class="grid-bg-container">
        <div class="plane">
            <div class="grid"></div>
            <div class="glow"></div>
        </div>
        <div class="plane">
            <div class="grid"></div>
            <div class="glow"></div>
        </div>
    </div>

    <header class="dynamic-header">
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-dark">
            <div class="container-fluid px-4">
                <a class="navbar-brand text-white fw-bold" asp-area="" asp-controller="Home" asp-action="Index">CTRL+ALT+SHAMIL</a>
                <button class="navbar-toggler collapsed" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse d-sm-inline-flex justify-content-center collapse">
                    <ul class="navbar-nav">
                        <li class="nav-item"><a class="nav-link text-white" asp-controller="Home" asp-action="Index">[HOME]</a></li>

                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle text-white" href="#" id="portfolioDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                [PORTFOLIO]
                            </a>
                            <ul class="dropdown-menu matrix-dropdown" aria-labelledby="portfolioDropdown">
                                <li><a class="dropdown-item" asp-controller="Project" asp-action="Index">[Projects]</a></li>
                                <li><a class="dropdown-item" asp-controller="CaseStudy" asp-action="Index">[Case Studies]</a></li>
                            </ul>
                        </li>

                        <li class="nav-item"><a class="nav-link text-white" asp-controller="Skills" asp-action="Index">[Technical Skills]</a></li>
                        <li class="nav-item"><a class="nav-link text-white" href="/docs/Alexander_Shamil_Mondoka_Resume.pdf" target="_blank">[CV_DOWNLOAD]</a></li>
                        <li class="nav-item"><a class="nav-link text-white" asp-controller="Contact" asp-action="Index">[CONTACT]</a></li>
                    </ul>

                    <div class="header-social-icons">
                        <a href="https://wa.link/aa5nld" target="_blank" title="WhatsApp"><i class="fab fa-whatsapp"></i></a>
                        <a href="https://www.linkedin.com/in/alexander-mondoka-0493bb217/" target="_blank" title="LinkedIn"><i class="fab fa-linkedin-in"></i></a>
                        <a href="https://github.com/TravisXO" target="_blank" title="GitHub"><i class="fab fa-github"></i></a>
                    </div>
                </div>
            </div>
        </nav>
    </header>



    <div class="stack-container">
        <div class="layout-grid">

            <main role="main" class="main-content">
                <!-- Dynamic Hero Section -->
                <section id="hero" class="stack-card hero-section" data-title="START">
                    <div id="dynamic-hero-container" class="container-fluid px-4 d-none">
                        <div class="row justify-content-center">
                            <div class="col-12 col-xl-10">
                                <div class="row align-items-center">
                                    <div class="col-lg-7 mb-4 mb-lg-0">
                                        <h1 id="hero-headline" class="display-4 fw-bold glitch-text font-monospace mb-3" style="color: var(--accent-color);" data-text=""></h1>
                                        <p id="hero-subheadline" class="lead font-monospace mb-4" style="color: white;"></p>
                                        <div id="hero-cta-buttons" class="d-flex flex-wrap gap-3 opacity-0" style="transition: opacity 1s;"></div>
                                    </div>
                                    <div class="col-lg-5">
                                        <div id="hero-stats-grid" class="row g-3 opacity-0" style="transition: opacity 1s 0.5s;"></div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </section>

                @RenderBody()

                <footer id="footer" class="stack-card footer-section text-muted" data-title="Footer">
                    <div class="container-fluid px-4 py-5 text-center">
                        <div class="row justify-content-center">
                            <div class="col-12 col-xl-10">
                                <div class="footer-socials mb-4 d-flex justify-content-center gap-4">
                                    <a href="#" class="text-white hover-accent"><i class="fab fa-github fa-xl"></i></a>
                                    <a href="#" class="text-white hover-accent"><i class="fab fa-linkedin fa-xl"></i></a>
                                </div>

                                <p class="mb-3 text-white">© 2025 ALEXANDER_SHAMIL_MONDOKA | ALL_RIGHTS_RESERVED</p>

                                <div class="footer-quick-links mb-4">
                                    <a asp-controller="Home" asp-action="Privacy" class="text-decoration-none text-muted hover-accent mx-2">[PRIVACY]</a>
                                    <a asp-controller="Home" asp-action="Terms" class="text-decoration-none text-muted hover-accent mx-2">[TERMS]</a>
                                </div>

                                <div class="footer-easter-egg small" style="color: var(--accent-color); font-family: var(--mono-font);">
                                    > TYPE 'HELP' FOR COMMANDS
                                </div>
                            </div>
                        </div>
                    </div>
                </footer>
            </main>

            <aside class="right-col d-none d-md-flex">
                <div class="quick-scroll-nav" id="quickScrollNav"></div>
            </aside>
        </div>
    </div>

    <canvas id="cursorCanvas"></canvas>

    <script>
        const canvas = document.getElementById('cursorCanvas');
        const ctx = canvas.getContext('2d');
        const SIZE = 80;
        canvas.width = SIZE;
        canvas.height = SIZE;

        Object.assign(canvas.style, {
            position: 'fixed',
            pointerEvents: 'none',
            zIndex: '99999',
            transform: 'translate(-4px, -4px)'
        });

        const cols = Math.floor(SIZE / 8);
        const drops = Array(cols).fill(0).map(() => Math.random() * -10);

        document.addEventListener('mousemove', e => {
            canvas.style.left = e.clientX + 'px';
            canvas.style.top  = e.clientY + 'px';
        });

        function getPointerPath() {
            const p = new Path2D();
            p.moveTo(6, 4);
            p.lineTo(6, 38);
            p.lineTo(14, 28);
            p.lineTo(19, 42);
            p.lineTo(24, 40);
            p.lineTo(19, 26);
            p.lineTo(30, 26);
            p.closePath();
            return p;
        }

        function draw() {
            ctx.clearRect(0, 0, SIZE, SIZE);

            const pointer = getPointerPath();

            // Fill pointer solid black background
            ctx.save();
            ctx.fillStyle = '#000000';
            ctx.fill(pointer);
            ctx.restore();

            // Clip ALL matrix rain to inside the pointer shape
            ctx.save();
            ctx.clip(pointer);

            ctx.font = 'bold 8px JetBrains Mono, monospace';
            for (let i = 0; i < drops.length; i++) {
                const char = Math.random() > 0.5 ? '1' : '0';
                ctx.fillStyle = i % 4 === 0 ? '#ffffff' : '#00ff41';
                ctx.shadowColor = '#00ff41';
                ctx.shadowBlur = 4;
                ctx.fillText(char, i * 8, drops[i] * 8);
                if (drops[i] * 8 > SIZE && Math.random() > 0.7) drops[i] = 0;
                drops[i] += 0.5;
            }

            ctx.restore();

            // Draw glowing green pointer outline on top
            ctx.save();
            ctx.strokeStyle = '#00ff41';
            ctx.lineWidth = 1.5;
            ctx.shadowColor = '#00ff41';
            ctx.shadowBlur = 12;
            ctx.stroke(pointer);
            ctx.restore();
        }

        setInterval(draw, 40);

        function updateClocks() {
            const now = new Date();
            const lusakaTime = new Intl.DateTimeFormat('en-GB', {
                timeZone: 'Africa/Lusaka',
                hour: '2-digit',
                minute: '2-digit',
                hour12: false
            }).format(now);

            const desktopClock = document.getElementById('lusakaTime');
            const mobileClock = document.getElementById('lusakaTimeMobile');

            if (desktopClock) desktopClock.textContent = lusakaTime;
            if (mobileClock) mobileClock.textContent = lusakaTime;
        }
        setInterval(updateClocks, 1000);
        updateClocks();
    </script>

    <script src="https://www.gstatic.com/firebasejs/11.6.1/firebase-app-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/11.6.1/firebase-auth-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/11.6.1/firebase-firestore-compat.js"></script>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    <script src="~/js/hero.js" asp-append-version="true"></script>

    @await RenderSectionAsync("Scripts", required: false)

</body>
</html>